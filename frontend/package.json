{
  "name": "goodcity-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build --ssrManifest --outDir dist/client && vite build --ssr src/entry-server.js --outDir dist/server",
    "preview": "NODE_ENV=production node server.js",
    "lint": "eslint src --ext .vue,.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ofetch": "^1.3.4",
    "pinia": "^2.1.7",
    "vue": "^3.4.21",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.0"
  }
}
